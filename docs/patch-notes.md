# Patch Notes

## v0.1.0 (2026-03-28)

### Features

- **cap001** — Initial scaffold: workspace discovery, Client with bearer auth + 401 retry, plugin system with snf-instance-rest adapter, code generation, @stage workflow decorator, CLI with workspace/plugin/workflow commands, VariableStore
